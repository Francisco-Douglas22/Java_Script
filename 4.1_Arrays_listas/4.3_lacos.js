const nome = 'Francisco Douglas';

for (let i=0; i < nome.length; i++){
    console.log(nome[i]);
}

//espaço exemplo mais simples
console.log('\n')

//outro exemplo mais simples
const nv_nome = 'Francisco';

for (let c = 0; c < nv_nome.length; c++){
    const letra = nv_nome[c];
    console.log(letra)
}

/* Explicando o For:
Quando digita for altomaticamente se vc aperta TAB aparecera assim.
for (let index = 0; index < array.length; index++) {
    const element = array[index];

}

Explicação
for = Loop
let = Variavel que ficara mudando sempre
index 0 = Sera a variavel criada no (let) e sera Primeiro Indice da variavel
index < = Sera a variavel criada no (let)Ultimo index sera utilizado quando voce aplica a variavel
array.length = Array e a Variavel e Length e o Tamanho da Sua variavel pode ser modificada
index ++ = ira intera a cada letra ou numero

Exemplo abaixo*/